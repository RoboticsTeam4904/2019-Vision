// mod contours;
// use contours::*;
use opencv::{
    prelude::*,
    // types::VectorOfint
    core::Mat
};

fn main() {
    // let mut b = VectorOfint::new(vec![1,2,3]);
    let mut m = Mat::default().unwrap();

}